/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * The Music Repository API
 * The Music Repository API — the single source of truth for the HTTP contract.
 * Compiles to OpenAPI 3.1 (`openapi.json`), which drives Orval (FE client + Zod) and the
 * backend Zod DTOs. Paths are declared ONLY here (never in NestJS decorators).
 * OpenAPI spec version: 0.0.0
 */
import type { DashboardBackgroundPref } from './dashboardBackgroundPref';
import type { DashboardWidget } from './dashboardWidget';

/**
 * A single named practice space.
 */
export interface DashboardSpace {
  /** Stable client-generated id, unique within the user's collection. */
  id: string;
  /** User-facing space name. */
  name: string;
  /** Optional icon name (design-system Icon registry). */
  icon?: string;
  /** Optional per-space animated background. */
  background?: DashboardBackgroundPref;
  /** The widgets placed on this space. */
  widgets: DashboardWidget[];
}
