/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * The Music Repository API
 * The Music Repository API — the single source of truth for the HTTP contract.
 * Compiles to OpenAPI 3.1 (`openapi.json`), which drives Orval (FE client + Zod) and the
 * backend Zod DTOs. Paths are declared ONLY here (never in NestJS decorators).
 * OpenAPI spec version: 0.0.0
 */
import type { ContentAdminSummaryStatus } from './contentAdminSummaryStatus';
import type { ContentAdminSummaryType } from './contentAdminSummaryType';
import type { ContentAdminSummaryVisibility } from './contentAdminSummaryVisibility';

/**
 * Admin list row (any status).
 */
export interface ContentAdminSummary {
  slug: string;
  title: string;
  type: ContentAdminSummaryType;
  status: ContentAdminSummaryStatus;
  visibility: ContentAdminSummaryVisibility;
  difficulty?: number;
  updatedAt: string;
  /** Premium plan that unlocks the item (`premium`/`pro`), if any. */
  tier?: string;
  /** Historical era from `details.era`, if set. */
  era?: string;
  /** Taxonomy slugs — enough to drive admin axes/facets; labels derive client-side. */
  genres: string[];
  instruments: string[];
}
