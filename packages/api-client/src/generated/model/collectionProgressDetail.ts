/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * The Music Repository API
 * The Music Repository API — the single source of truth for the HTTP contract.
 * Compiles to OpenAPI 3.1 (`openapi.json`), which drives Orval (FE client + Zod) and the
 * backend Zod DTOs. Paths are declared ONLY here (never in NestJS decorators).
 * OpenAPI spec version: 0.0.0
 */
import type { CollectionEntry } from './collectionEntry';
import type { CollectionFacets } from './collectionFacets';
import type { CollectionProgressDetailCurationType } from './collectionProgressDetailCurationType';
import type { CollectionProgressDetailKind } from './collectionProgressDetailKind';
import type { CollectionProgressDetailStatus } from './collectionProgressDetailStatus';
import type { CollectionProgressDetailVisibility } from './collectionProgressDetailVisibility';
import type { CollectionSectionView } from './collectionSectionView';

/**
 * Collection detail with per-item completion + overall progress (auth-aware).
 */
export interface CollectionProgressDetail {
  slug: string;
  title: string;
  summary?: string;
  kind: CollectionProgressDetailKind;
  visibility: CollectionProgressDetailVisibility;
  status: CollectionProgressDetailStatus;
  curationType: CollectionProgressDetailCurationType;
  itemCount: number;
  featured: boolean;
  difficultyMin?: number;
  difficultyMax?: number;
  estMinutes?: number;
  curatorName?: string;
  heroImageKey?: string;
  accent?: string;
  tags?: string[];
  facets?: CollectionFacets;
  popularity: number;
  averageRating?: number;
  ratingCount: number;
  /** Internal id — used by the translations admin to key per-locale collection translations. */
  id: string;
  ownerId?: string;
  bodyMdx?: string;
  outcomes?: string[];
  curatorBio?: string;
  items: CollectionEntry[];
  sections: CollectionSectionView[];
  percentComplete: number;
  completedCount: number;
  nextUpSlug: string | null;
}
