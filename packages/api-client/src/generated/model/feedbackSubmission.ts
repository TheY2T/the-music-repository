/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * The Music Repository API
 * The Music Repository API — the single source of truth for the HTTP contract.
 * Compiles to OpenAPI 3.1 (`openapi.json`), which drives Orval (FE client + Zod) and the
 * backend Zod DTOs. Paths are declared ONLY here (never in NestJS decorators).
 * OpenAPI spec version: 0.0.0
 */
import type { FeedbackStatus } from './feedbackStatus';
import type { FeedbackType } from './feedbackType';

/**
 * A feedback submission as seen by an admin during triage.
 */
export interface FeedbackSubmission {
  id: string;
  type: FeedbackType;
  title?: string;
  message: string;
  status: FeedbackStatus;
  /** Better Auth id of the submitter. */
  userId: string;
  /** Submitter email, included in the admin view for close-the-loop follow-up. */
  userEmail?: string;
  locale?: string;
  /** Page the submission was sent from (captured for bug reports). */
  pageUrl?: string;
  /** Browser user-agent (captured for bug reports). */
  userAgent?: string;
  /** Whether the item is surfaced on the public /roadmap board. */
  isPublic: boolean;
  upvoteCount: number;
  adminNotes?: string;
  createdAt: string;
  updatedAt: string;
}
