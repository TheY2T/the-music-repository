/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * The Music Repository API
 * The Music Repository API — the single source of truth for the HTTP contract.
 * Compiles to OpenAPI 3.1 (`openapi.json`), which drives Orval (FE client + Zod) and the
 * backend Zod DTOs. Paths are declared ONLY here (never in NestJS decorators).
 * OpenAPI spec version: 0.0.0
 */
import type { InstrumentPreferencesInputHandedness } from './instrumentPreferencesInputHandedness';

/**
 * The user's instrument preferences (input body).
 */
export interface InstrumentPreferencesInput {
  /** Guitar orientation: `right` (default) or `left` mirrors the fretboard + chord diagrams. */
  handedness: InstrumentPreferencesInputHandedness;
  /** Chosen piano skin id (see the keyboard skin registry). */
  keyboardSkin: string;
  /** Chosen guitar/fretboard skin id (see the fretboard skin registry). */
  fretboardSkin: string;
  /** Whether the tools open in fullscreen by default. */
  fullscreen: boolean;
}
