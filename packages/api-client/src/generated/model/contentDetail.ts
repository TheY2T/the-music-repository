/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * The Music Repository API
 * The Music Repository API — the single source of truth for the HTTP contract.
 * Compiles to OpenAPI 3.1 (`openapi.json`), which drives Orval (FE client + Zod) and the
 * backend Zod DTOs. Paths are declared ONLY here (never in NestJS decorators).
 * OpenAPI spec version: 0.0.0
 */
import type { ContentDetails } from './contentDetails';
import type { ContentDetailType } from './contentDetailType';
import type { ContentDetailVisibility } from './contentDetailVisibility';
import type { ContentEmbed } from './contentEmbed';
import type { MediaAsset } from './mediaAsset';
import type { TaxonomyRef } from './taxonomyRef';

/**
 * Full detail view of a catalogue item.
 */
export interface ContentDetail {
  slug: string;
  title: string;
  summary?: string;
  type: ContentDetailType;
  difficulty?: number;
  visibility: ContentDetailVisibility;
  /** For premium items: which plan unlocks it — "premium" or "pro". */
  tier?: string;
  /** True when this is premium content the current viewer is not entitled to (body/media withheld). */
  locked?: boolean;
  genres: TaxonomyRef[];
  instruments: TaxonomyRef[];
  topics: TaxonomyRef[];
  /** Internal id — used by the translations admin to key per-locale content translations. */
  id: string;
  bodyMdx?: string;
  source?: string;
  attribution?: string;
  license?: string;
  details?: ContentDetails;
  /** Preconfigured interactive tools to render below the prose (authored, from `details` JSONB). */
  embeds?: ContentEmbed[];
  tags: TaxonomyRef[];
  media: MediaAsset[];
  createdAt: string;
  updatedAt: string;
}
