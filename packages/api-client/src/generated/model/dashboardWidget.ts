/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * The Music Repository API
 * The Music Repository API — the single source of truth for the HTTP contract.
 * Compiles to OpenAPI 3.1 (`openapi.json`), which drives Orval (FE client + Zod) and the
 * backend Zod DTOs. Paths are declared ONLY here (never in NestJS decorators).
 * OpenAPI spec version: 0.0.0
 */
import type { DashboardWidgetConfig } from './dashboardWidgetConfig';

/**
 * One widget placed on a space's grid. `config` is a widget-type-specific bag (mirrors the embed field vocabulary).
 */
export interface DashboardWidget {
  /** Stable client-generated id, unique within the space. */
  id: string;
  /** Widget type key (resolved by the widget registry). */
  type: string;
  /** Grid column (0-based). */
  x: number;
  /** Grid row (0-based). */
  y: number;
  /** Width in grid columns. */
  w: number;
  /** Height in grid rows. */
  h: number;
  /** Widget-type-specific configuration. */
  config: DashboardWidgetConfig;
}
