// @testing-library/jest-dom matchers (toBeInTheDocument, etc.) for component tests. Being under
// `src`, this also makes the Vitest Assertion augmentation visible to `tsc`.
import '@testing-library/jest-dom/vitest';
