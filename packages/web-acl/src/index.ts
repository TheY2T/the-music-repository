// @TheY2T/tmr-web-acl — the web data seam: credentialed API-client wrappers, the Better Auth
// browser client, shell config (nav), and shared shell types (Flags/User/Locale). No UI. Both
// @TheY2T/tmr-common-ui and @TheY2T/tmr-musickit-ui depend on this layer (keeps the UI DAG acyclic).
export * from './types';
