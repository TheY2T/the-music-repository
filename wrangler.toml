name = "theusicrepository"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"
 
# Non-secret Pages Function variables. This project is configured via
# wrangler.toml, so plaintext vars are managed HERE (the dashboard shows them
# read-only). RESEND_API_KEY is a SECRET — do NOT put it here. Set it with
#   wrangler pages secret put RESEND_API_KEY
# (or the dashboard's encrypted "secret" option), and in .dev.vars for local dev.
[vars]
CONTACT_TO = "michael.hewett87@gmail.com"
CONTACT_FROM = "Contact <contact@themusicrepository.com>"
themusicrepository.com