---
import BackingTrack from '@/components/BackingTrack.tsx';
import InfoView from '@/components/InfoView.tsx';
import '@/styles/global.css';

if (!Astro.locals.flags.toolBackingTrack) {
  return Astro.redirect('/tools');
}
const showInfoView = Astro.locals.flags.infoView;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Backing track — The Music Repository</title>
    <script is:inline>
      const stored = localStorage.getItem('theme');
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      if (stored === 'dark' || (!stored && prefersDark)) {
        document.documentElement.classList.add('dark');
      }
    </script>
  </head>
  <body>
    <main class="mx-auto max-w-4xl space-y-6 p-8">
      <header class="space-y-1">
        <a href="/tools" class="text-sm text-muted-foreground underline">← Tools</a>
        <h1 class="text-3xl font-bold">Backing track</h1>
        <p class="text-muted-foreground">
          A looping band to jam over — pick a progression, key, and tempo.
        </p>
      </header>
      <BackingTrack client:load />
    </main>
    {showInfoView && <InfoView client:idle />}
  </body>
</html>
