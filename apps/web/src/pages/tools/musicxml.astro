---
import { localizedPath } from '@TheY2T/tmr-i18n';

// Redirects to /tools/score — the score playground accepts MusicXML directly (alphaTab sniffs the format).
return Astro.redirect(localizedPath(Astro.locals.locale, '/tools/score'));
---
