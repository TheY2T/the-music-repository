---
import InfoView from '@/components/InfoView.tsx';
import TuningReference from '@/components/TuningReference.tsx';
import '@/styles/global.css';

if (!Astro.locals.flags.toolTuner) {
  return Astro.redirect('/tools');
}
const showInfoView = Astro.locals.flags.infoView;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Tuning reference — The Music Repository</title>
    <script is:inline>
      const stored = localStorage.getItem('theme');
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      if (stored === 'dark' || (!stored && prefersDark)) {
        document.documentElement.classList.add('dark');
      }
    </script>
  </head>
  <body>
    <main class="mx-auto max-w-xl space-y-6 p-8">
      <header class="space-y-1">
        <a href="/tools" class="text-sm text-muted-foreground underline">← Tools</a>
        <h1 class="text-3xl font-bold">Tuning reference</h1>
        <p class="text-muted-foreground">Play reference tones to tune by ear.</p>
      </header>
      <TuningReference client:load />
    </main>
    {showInfoView && <InfoView client:idle />}
  </body>
</html>
