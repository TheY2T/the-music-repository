---
import InfoView from '@/components/InfoView.tsx';
import ScaleBoxes from '@/components/ScaleBoxes.tsx';
import '@/styles/global.css';

if (!Astro.locals.flags.toolScaleBoxes) {
  return Astro.redirect('/tools');
}
const showInfoView = Astro.locals.flags.infoView;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Scale positions — The Music Repository</title>
    <script is:inline>
      const stored = localStorage.getItem('theme');
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      if (stored === 'dark' || (!stored && prefersDark)) {
        document.documentElement.classList.add('dark');
      }
    </script>
  </head>
  <body>
    <main class="mx-auto max-w-5xl space-y-6 p-8">
      <header class="space-y-1">
        <a href="/tools" class="text-sm text-muted-foreground underline">← Tools</a>
        <h1 class="text-3xl font-bold">Scale positions</h1>
        <p class="text-muted-foreground">See any scale as a movable box on the fretboard.</p>
      </header>
      <ScaleBoxes client:load />
    </main>
    {showInfoView && <InfoView client:idle />}
  </body>
</html>
