---
import HelpTopicForm from '@/components/HelpTopicForm.tsx';
import { guardAdmin } from '@/lib/admin-guard';
import '@/styles/global.css';

const redirect = guardAdmin(Astro);
if (redirect) {
  return redirect;
}
if (!Astro.locals.flags.infoView) {
  return Astro.redirect('/admin');
}
const { slug } = Astro.params;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Edit help topic — Admin</title>
    <script is:inline>
      const stored = localStorage.getItem('theme');
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      if (stored === 'dark' || (!stored && prefersDark)) {
        document.documentElement.classList.add('dark');
      }
    </script>
  </head>
  <body>
    <main class="mx-auto max-w-2xl space-y-6 p-8">
      <header class="space-y-1">
        <a href="/admin/help" class="text-sm text-muted-foreground underline">← Help topics</a>
        <h1 class="text-3xl font-bold">Edit help topic</h1>
      </header>
      <HelpTopicForm client:load slug={slug} />
    </main>
  </body>
</html>
