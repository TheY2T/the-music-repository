---
import AdminHelpList from '@/components/AdminHelpList.tsx';
import BaseLayout from '@/layouts/BaseLayout.astro';
import { guardAdmin } from '@/lib/admin-guard';
import { localizedPath, t } from '@TheY2T/tmr-i18n';

const redirect = guardAdmin(Astro);
if (redirect) {
  return redirect;
}
if (!Astro.locals.flags.infoView) {
  return Astro.redirect('/admin');
}
const locale = Astro.locals.locale;
---

<BaseLayout title={`${t(locale, 'admin.helpTopics')} — ${t(locale, 'nav.admin')}`}>
  <main class="mx-auto max-w-4xl space-y-6 p-8">
    <header class="space-y-1">
      <a href={localizedPath(locale, '/admin')} class="text-sm text-muted-foreground underline">
        {t(locale, 'common.backContent')}
      </a>
      <h1 class="text-3xl font-bold">{t(locale, 'admin.helpTopics')}</h1>
      <p class="text-sm text-muted-foreground">{t(locale, 'admin.helpSubtitle')}</p>
    </header>
    <AdminHelpList client:load locale={locale} />
  </main>
</BaseLayout>
