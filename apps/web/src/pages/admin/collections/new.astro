---
import CollectionForm from '@/components/CollectionForm.tsx';
import BaseLayout from '@/layouts/BaseLayout.astro';
import { guardAdmin } from '@/lib/admin-guard';
import { localizedPath, t } from '@TheY2T/tmr-i18n';

const redirect = guardAdmin(Astro);
if (redirect) {
  return redirect;
}
if (!Astro.locals.flags.collections) {
  return Astro.redirect('/admin');
}
const locale = Astro.locals.locale;
---

<BaseLayout title={`${t(locale, 'admin.newCollection')} — ${t(locale, 'nav.admin')}`}>
  <main class="mx-auto max-w-2xl space-y-6 p-8">
    <header class="space-y-1">
      <a href={localizedPath(locale, '/admin/collections')} class="text-sm text-muted-foreground underline">
        {t(locale, 'common.backCollections')}
      </a>
      <h1 class="text-3xl font-bold">{t(locale, 'admin.newCollection')}</h1>
    </header>
    <CollectionForm client:load locale={locale} />
  </main>
</BaseLayout>
