---
import AdminFaqManager from '@TheY2T/tmr-common-ui/AdminFaqManager';
import BaseLayout from '@/layouts/BaseLayout.astro';
import { guardAdmin } from '@/lib/admin-guard';
import { localizedPath, t } from '@TheY2T/tmr-i18n';
import PageShell from '@TheY2T/tmr-ui/astro/PageShell.astro';

const redirect = guardAdmin(Astro);
if (redirect) {
  return redirect;
}
if (!Astro.locals.flags.faq) {
  return Astro.redirect('/admin');
}
const locale = Astro.locals.locale;
---

<BaseLayout noindex title={`${t(locale, 'admin.faq')} — ${t(locale, 'nav.admin')}`}>
  <PageShell
    title={t(locale, 'admin.faq')}
    subtitle={t(locale, 'admin.faqSubtitle')}
    back={{ href: localizedPath(locale, '/admin'), label: t(locale, 'common.backContent') }}
  >
    <AdminFaqManager client:load locale={locale} />
  </PageShell>
</BaseLayout>
