# alphaTab copies the Bravura font + SONiVOX soundfont here on every dev/build (see astro.config.mjs),
# so these are generated assets, not sources.
public/font/
public/soundfont/

# Playwright
/test-results/
/playwright-report/
/e2e/.auth/
